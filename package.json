{
  "name": "pi-tin-workspace",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "corepack pnpm --filter @pi-tin/desktop dev",
    "tauri": "corepack pnpm --filter @pi-tin/desktop tauri",
    "build": "corepack pnpm -r build",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
